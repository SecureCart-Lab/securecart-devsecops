spring:
  datasource:
    url: jdbc:h2:mem:securecart;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
securecart:
  security:
    user: { username: student, password: test-password }
    admin: { username: admin, password: test-admin-password }
